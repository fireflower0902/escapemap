name: 방탈출 스케줄 크롤링

on:
  schedule:
    - cron: "0 * * * *"     # 1시간마다 — 스케줄만 갱신 (SKIP_META_WRITES=true, delta write)
    - cron: "0 15 * * *"    # 매일 자정 KST (UTC 15:00) — 카페/테마 포함 전체 동기화
  workflow_dispatch:        # 수동 실행 버튼

# ── 공통 설정 ─────────────────────────────────────────────────────────────────
# 16개 job 병렬 실행 → 벽시계 시간 ~16배 단축 (목표: 최장 job ≤ 15분)
# Naver Booking 55개 지점 → 11개씩 5분할 (각 ~13분)
# setup 시간(checkout + uv sync): job당 약 40~60초 소요

env:
  TZ: Asia/Seoul   # 모든 job/step에 KST 적용 → datetime.now(), date.today() KST 기준

jobs:

  # ── Job 1: 셜록홈즈 (지점 多, 단독 실행) ─────────────────────────────────
  crawl-1:
    name: "크롤링 1 — 셜록홈즈"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_sherlock_db.py

  # ── Job 2: 마스터키 (지점 多, 단독 실행) ─────────────────────────────────
  crawl-2:
    name: "크롤링 2 — 마스터키"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_masterkey_db.py

  # ── Job 3: 머더파커 + xcape (지점 多) ────────────────────────────────────
  crawl-3:
    name: "크롤링 3 — 머더파커 + xcape"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_murderparker_db.py
          run_script sync_xcape_db.py

  # ── Job 4: Naver Booking — 인덱스 0~10 (11개 지점) ───────────────────────
  crawl-4:
    name: "크롤링 4 — Naver Booking (0~10)"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          uv run python scripts/sync_escapetop_db.py --start 0 --end 11

  # ── Job 5: Naver Booking — 인덱스 11~21 (11개 지점) ──────────────────────
  crawl-5:
    name: "크롤링 5 — Naver Booking (11~21)"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          uv run python scripts/sync_escapetop_db.py --start 11 --end 22

  # ── Job 6: Naver Booking — 인덱스 22~32 (11개 지점) ──────────────────────
  crawl-6:
    name: "크롤링 6 — Naver Booking (22~32)"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          uv run python scripts/sync_escapetop_db.py --start 22 --end 33

  # ── Job 7: Naver Booking — 인덱스 33~43 (11개 지점) ──────────────────────
  crawl-7:
    name: "크롤링 7 — Naver Booking (33~43)"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          uv run python scripts/sync_escapetop_db.py --start 33 --end 44

  # ── Job 8: Naver Booking — 인덱스 44~ (11개 지점) ────────────────────────
  crawl-8:
    name: "크롤링 8 — Naver Booking (44~)"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          uv run python scripts/sync_escapetop_db.py --start 44

  # ── Job 9: sinbiweb (9개 사이트, 단독 실행) ──────────────────────────────
  crawl-9:
    name: "크롤링 9 — sinbiweb"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_sinbiweb_db.py

  # ── Job 10: 서울 홍대/마포/신촌 계열 ────────────────────────────────────
  crawl-10:
    name: "크롤링 10 — 홍대/마포/신촌"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_keyescape_db.py
          run_script sync_frankantique_db.py
          run_script sync_dpsnnn_db.py
          run_script sync_clevertown_db.py
          run_script sync_rabbithole_db.py
          run_script sync_roomlescape_db.py
          run_script sync_ledasquare_db.py
          run_script sync_deepthinker_db.py
          run_script sync_showroom404_db.py
          run_script sync_horrorswitch_db.py
          run_script sync_mysteryroom_db.py
          run_script sync_xphobia_db.py

  # ── Job 11: 서울 강남/잠실/건대 + 큐브 ──────────────────────────────────
  crawl-11:
    name: "크롤링 11 — 강남/잠실/건대 + 큐브"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_cubeescape_db.py
          run_script sync_xdungeon_db.py
          run_script sync_mafiacafe_db.py
          run_script sync_jigobyeol_db.py
          run_script sync_seoulesc_db.py
          run_script sync_zeroworld_db.py
          run_script sync_signescape_db.py
          run_script sync_studioesc_db.py
          run_script sync_nextedition_db.py
          run_script sync_play33_db.py
          run_script sync_seoulescaperoom_db.py

  # ── Job 12: 경기/인천/지방 계열 ─────────────────────────────────────────
  crawl-12:
    name: "크롤링 12 — 경기/인천/지방"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_escapers_db.py
          run_script sync_lastescape_db.py
          run_script sync_dumbndumber_db.py
          run_script sync_secretcode_db.py
          run_script sync_escapecity_db.py
          run_script sync_teraspace_db.py
          run_script sync_xescape_db.py
          run_script sync_puzzlefactory_db.py

  # ── Job 13: playthe.world 계열 + 소규모 1 ───────────────────────────────
  crawl-13:
    name: "크롤링 13 — playtheworld + 소규모1"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_doorescape_db.py
          run_script sync_playtheworld_etc_db.py
          run_script sync_fantastrick_db.py
          run_script sync_roomescape_db.py
          run_script sync_ttescape_db.py
          run_script sync_panicescape_db.py
          run_script sync_themazegd_db.py
          run_script sync_thetrapsy_db.py
          run_script sync_escapehouse_db.py
          run_script sync_conanescape_db.py
          run_script sync_dreampicnic_db.py

  # ── Job 14: 소규모 2a ────────────────────────────────────────────────────
  crawl-14:
    name: "크롤링 14 — 소규모2a"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_movieescape_db.py
          run_script sync_no1escape_db.py
          run_script sync_oasisescape_db.py
          run_script sync_thecode_db.py
          run_script sync_thetimeescape_db.py
          run_script sync_zescape_db.py

  # ── Job 15: 소규모 2b ────────────────────────────────────────────────────
  crawl-15:
    name: "크롤링 15 — 소규모2b"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_playescape_db.py
          run_script sync_geniusescape_db.py
          run_script sync_trickescape_db.py
          run_script sync_escbangtalul_db.py
          run_script sync_theqescape_db.py
          run_script sync_escapedetective_db.py

  # ── Job 16: 소규모 3 + 지방 ─────────────────────────────────────────────
  crawl-16:
    name: "크롤링 16 — 소규모3 + 지방"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - run: uv sync --frozen
      - run: echo '${{ secrets.FIREBASE_CREDENTIALS_JSON }}' > firebase_credentials.json
      - name: Write .env
        run: |
          echo "DATABASE_URL=sqlite+aiosqlite:///./escape_aggregator.db" >> .env
          echo "FIREBASE_CREDENTIALS_PATH=./firebase_credentials.json" >> .env
          echo "ENVIRONMENT=production" >> .env
          if [ "${{ github.event.schedule }}" != "0 15 * * *" ]; then
            echo "SKIP_META_WRITES=true" >> .env
          fi
      - name: 크롤링 실행
        run: |
          run_script() {
            local script="$1"
            echo "::group::$script"
            uv run python "scripts/$script" \
              && echo "✅ $script 완료" \
              || echo "::warning::❌ $script 크롤링 실패 (exit $?)"
            echo "::endgroup::"
          }
          run_script sync_movieescape_cokr_db.py
          run_script sync_aliceescape_db.py
          run_script sync_vescape_db.py
          run_script sync_reverseesc_db.py
          run_script sync_codek_db.py
          run_script sync_rememberesc_db.py
          run_script sync_orangetb_db.py
